post_install() {
  udevadm control --reload-rules >/dev/null 2>&1 || true
  printf '%s\n' 'IO Center: Dark Mount einmal trennen und neu verbinden.'
}

post_upgrade() {
  post_install
}

post_remove() {
  udevadm control --reload-rules >/dev/null 2>&1 || true
}
